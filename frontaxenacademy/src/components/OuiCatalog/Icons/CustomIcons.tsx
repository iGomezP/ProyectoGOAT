export const InicioIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/InicioColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/InicioNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const ClasesIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/ClasesColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/ClasesNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const HorariosIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/HorariosColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/HorariosNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const CalificacionesIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/CalificacionesColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/CalificacionesNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const CalendarioIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/CalendarioColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/CalendarioNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const PagosIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/PagosColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/CalendarioNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const FotografiasIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/FotografiasColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/FotografiasNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const NotificacionesIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/NotificacionesColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/NotificacionesNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}

export const AdministracionIcon = (props: any) => {
    return (
        <>
            {props.color ? (
                <img
                    src="/src/assets/images/Icons/AdministracionColor.png"
                    alt=""

                    className={`all-icons ${props.color ? '' : 'hidden'}`}
                />
            ) : (
                <img
                    src="/src/assets/images/Icons/AdministracionNoColor.png"
                    alt=""

                    className={`all-icons ${props.color ? 'hidden' : ''}`}
                />
            )}
        </>
    );
}